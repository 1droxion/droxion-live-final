workflows:
  ios-capacitor:
    name: Droxion iOS Build
    max_build_duration: 60

    # Force Codemagic to use a compatible machine that has Xcode 15.4
    instance_type: mac_mini_m2          # ✅ known-good for Xcode 15.x
    environment:
      vars:
        APP_NAME: Droxion
        BUNDLE_ID: com.droxion.app
        WEB_DIR: dist
        IOS_PROJECT_PATH: ios/App
      node: 20
      xcode: 15.4                       # ✅ pin to available version
      cocoapods: default
      ios_signing:
        distribution_type: app_store
        bundle_identifier: com.droxion.app

    cache:
      cache_paths:
        - ~/.npm
        - ~/Library/Caches/CocoaPods
      cache_clear: true                 # clear stale cache

    scripts:
      - name: Show tools
        script: |
          node -v
          npm -v
          xcodebuild -version
          pod --version || true

      - name: Install dependencies
        script: npm ci

      - name: Build web app
        script: npm run build

      - name: Sync Capacitor iOS
        script: npx cap sync ios

      - name: Install CocoaPods
        script: |
          cd "$IOS_PROJECT_PATH"
          pod repo update
          pod install

      - name: Configure signing (from Codemagic)
        script: |
          keychain initialize
          app-store-connect fetch-signing-files "$BUNDLE_ID" --type IOS_APP_STORE --create
          keychain add-certificates
          xcode-project use-profiles

      - name: Increment build number
        script: |
          cd "$IOS_PROJECT_PATH"
          agvtool new-version -all $(date +%s)

      - name: Build IPA
        script: |
          xcode-project build-ipa \
            --workspace "$IOS_PROJECT_PATH/App.xcworkspace" \
            --scheme "App"

    artifacts:
      - $CM_EXPORT_DIR/*.ipa
      - $CM_EXPORT_DIR/*.dSYM.zip
      - $CM_EXPORT_DIR/*.xcarchive

    # Uncomment to auto-upload to TestFlight (after adding API key JSON in Codemagic env)
    # publishing:
    #   app_store_connect:
    #     auth: api_key
    #     api_key: $APP_STORE_CONNECT_API_KEY_JSON
    #     submit_to_testflight: true